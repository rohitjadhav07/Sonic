{
  "name": "astra-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Astra AI on Sonic Network",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network sonic-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network sonic-mainnet",
    "test": "hardhat test",
    "verify": "VERIFY_CONTRACTS=true hardhat run scripts/deploy.js --network sonic-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.3.1"
  }
}